package ${config.basePackage}.${config.dtoPackageName};

#foreach($import in $definition.getExternalImports())
import $import;
#end

/**
 * @ClassName ${definition.name}
 * @Description ${definition.description}
 * @Author api-codegen
 **/
#foreach($annotation in $definition.getValidateAnnotations())
${annotation.toString()}
#end
public class ${definition.name} {

#foreach($field in $definition.fields)

    /**
     * ${field.description}
     */
#foreach($annotation in $field.getValidateAnnotations())
    ${annotation.toString()}
#end
    private ${field.type} ${field.name}$field.value();
#end

}